<%= form_for(@minipost) do |f| %>
  <% if @minipost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@minipost.errors.count, "error") %> prohibited this minipost from being saved:</h2>

      <ul>
      <% @minipost.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
